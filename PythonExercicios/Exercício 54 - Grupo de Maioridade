contador = 0

for c in range(1,8):
    idade = int(input('Digite a {}º idade: '.format(c)))
    if idade > 18:
        contador = contador + 1
print('Das pessoas que informaram a idade, {} estão abaixo dos 18 anos'.format(contador))